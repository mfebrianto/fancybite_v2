%table
  %thead
    %tr
      %th
        name
      %th
        description
      %th
        price
  %tbody
    - @all_menu.each do |menu|
      %tr
        %td
          = link_to menu.name, admin_menu_path(menu.id)
        %td
          = menu.description
        %td
          = menu.price

= link_to 'add menu', new_admin_menu_path